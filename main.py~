import os
import _thread
import time
import importlib

def connect(token):
    

if __name__ == '__main__':
    while 1:
        time.sleep(1)
        for token in os.scandir(os.getcwd() + os.sep + 'tokens'):
            token=open(os.getcwd() + os.sep + 'tokens' + os.sep + token.name, 'r').read().strip())
            
    #main.main(open(os.getcwd()+os.sep+'token', 'r').read().strip())
